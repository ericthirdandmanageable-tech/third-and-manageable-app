# Dependencies
node_modules/
.pnp
.pnp.*
.venv/
__pycache__/
*.py[cod]

# Builds
dist/
build/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Environment — never commit real credentials (see VERCEL_MIGRATION_PLAN.md §6.2)
.env
.env.*
!.env.example
!env.example

# Databases — local dev only, never deployed (§1.2 #8)
*.db
*.sqlite
*.sqlite3

# Secrets — service-account keys and certs must never be committed (§6.2)
*-firebase-adminsdk-*.json
*.pem

# Test / tooling caches
.pytest_cache/
.ruff_cache/
coverage/
.turbo/
firebase-debug.log
firestore-debug.log

# Vercel
.vercel/

# Editor / OS
.DS_Store
*.swp
.idea/

# Local artifacts
kickbacks-v2.vsix
